## Parent
[PRD-mvp-polish.md](../PRD-mvp-polish.md)

## What to build
Update the `SummaryView` in `ActiveMatchScreen.tsx` to match the visual style of the player selection screen.

1. **Row-based Leaderboard**: Replace the simple leaderboard rows with a richer component that mimics the `playerItem` style from `SelectPlayersScreen`.
2. **Components**:
    - Avatar/Initial circle.
    - Player name.
    - Total score (Strokes and Diff vs PAR) prominently displayed on the right.
3. **Consistency**: Ensure typography and spacing align with the rest of the application's polished UI.

## Acceptance criteria
- [ ] The leaderboard in the summary view looks like a high-fidelity list of players.
- [ ] Scores are easy to read and color-coded.
- [ ] Layout is consistent with the "Squad" selection screen.

## Blocked by
None - can start immediately.

Status: Completed

## What to build
Replace the native `react-native-maps` implementation with a `react-native-webview` based Leaflet.js map. This ensures a 100% free solution without requiring Google Maps API keys or native binary rebuilds.

## Acceptance criteria
- [x] Use `react-native-webview` to render a Leaflet map.
- [x] Show Tee marker (Yellow) and Basket marker (Orange).
- [x] Draw a dashed line between the two points.
- [x] Auto-fit the map bounds to show both markers.
- [x] Dark mode filter applied to OpenStreetMap tiles.

## Blocked by
None - can start immediately.

## What to build
Insert "SUM" tabs into the hole navigation flow of `ActiveMatchScreen`. These tabs should appear after Hole 9 and at the end of the match.

## Acceptance criteria
- [x] "SUM" tab appears after Hole 9 for 18-hole layouts.
- [x] "SUM" tab appears at the end of the round.
- [x] Tapping a "SUM" tab correctly toggles between `Map/Scorecard` and `SummaryView`.
- [x] Navigation state management handles both `hole` and `summary` types.

## Blocked by
None - can start immediately.
